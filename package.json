{
  "name": "pi-hive",
  "version": "1.0.0",
  "description": "Hierarchical Agent Swarm for Pi.dev",
  "main": "src/index.ts",
  "pi": {
    "extension": "src/index.ts",
    "skills": [
      "skills/hive/SKILL.md"
    ]
  },
  "scripts": {
    "install-local": "mkdir -p ~/.pi/agent/extensions && cp src/index.ts ~/.pi/agent/extensions/hive.ts && mkdir -p ~/.pi/skills/hive && cp skills/hive/SKILL.md ~/.pi/skills/hive/SKILL.md"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
