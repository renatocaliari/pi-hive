{
  "name": "pi-hive",
  "version": "1.0.0",
  "description": "Hierarchical Agent Swarm for Pi.dev",
  "main": "src/index.ts",
  "pi": {
    "extension": "src/index.ts",
    "skills": [
      "skills/hive.md"
    ]
  },
  "scripts": {
    "install-local": "mkdir -p ~/.pi/agent/extensions && cp src/index.ts ~/.pi/agent/extensions/hive.ts && mkdir -p ~/.pi/agent/skills && cp skills/hive.md ~/.pi/agent/skills/hive.md"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
